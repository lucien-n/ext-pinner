{
	"name": "Pinner - Persisted pinned tabs",
	"description": "Organize your pinned tabs into named collections. Save, restore, and automatically load them whenever you need. Import from and export to popular similar extensions like Save Pinned Tabs. Also keeps the muted state of a tab",
	"version": "1.1.3",
	"manifest_version": 3,
	"action": {
		"default_popup": "index.html",
		"default_icon": "icon.png"
	},
	"permissions": [
		"storage",
		"tabs"
	],
	"background": {
		"service_worker": "background.js",
		"type": "module"
	}
}